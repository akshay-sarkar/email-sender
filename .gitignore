# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.yalc/
yalc.lock

# Environment variables
.env

# Attachment files — do NOT ignore the attachments/ folder.
# Files placed there (e.g. resume PDFs) must be committed so Vercel
# can bundle them at build time. Only ignore OS junk inside it.
attachments/.DS_Store
