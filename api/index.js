// Vercel serverless entry point — imports the shared Express app and exports it as the handler.
// On local development, server/index.js is used instead.
import app from "../server/app.js"

export default app
